const labels = { health: 'Saúde', education: 'Educação', money: 'Dinheiro', stress: 'Estresse', communitySupport: 'Apoio social' };
function Meter({ label, value }) { const percentage = Math.max(0, Math.min(100, value * 10)); return <div><div className="flex justify-between text-xs font-semibold text-amber-50/90 mb-1"><span>{label}</span><span>{value}/10</span></div><div className="h-2 rounded-full bg-black/25 overflow-hidden"><div className="h-full rounded-full bg-amber-300 transition-all" style={{ width: `${percentage}%` }} /></div></div>; }
export default function ResourcePanel({ state }) { return <section className="glass rounded-3xl p-4 text-white bg-black/20"><h2 className="text-xs font-black uppercase tracking-[0.18em] text-amber-100 mb-3">Recursos da jornada</h2><div className="space-y-3">{Object.entries(labels).map(([key, label]) => <Meter key={key} label={label} value={state[key]} />)}</div></section>; }