import { motion } from 'framer-motion';
function EffectSummary({ effect }) { const labelMap = { position: 'casas', health: 'saúde', education: 'educação', money: 'dinheiro', stress: 'estresse', communitySupport: 'apoio social' }; const parts = Object.entries(effect).filter(([, value]) => value !== 0).map(([key, value]) => `${value > 0 ? '+' : ''}${value} ${labelMap[key]}`); return <p className="text-xs text-slate-500 mt-2">{parts.join(' • ')}</p>; }
export default function EventCard({ event, onChoose }) { return <div className="fixed inset-0 z-30 bg-black/60 backdrop-blur-sm flex items-center justify-center p-4"><motion.section initial={{ opacity: 0, scale: 0.9, y: 20 }} animate={{ opacity: 1, scale: 1, y: 0 }} className="max-w-lg w-full glass rounded-[2rem] overflow-hidden"><header className="p-6 text-center text-white" style={{ background: 'linear-gradient(135deg, #92400e, #d97706)' }}><p className="text-5xl mb-3" aria-hidden="true">{event.icon}</p><p className="text-xs uppercase tracking-[0.18em] text-amber-100 font-black">{event.category}</p><h2 className="text-2xl font-black mt-2">Decisão importante</h2></header><div className="p-6"><p className="text-slate-700 leading-7 mb-6">{event.situation}</p><div className="space-y-3">{event.choices.map((choice) => <button key={choice.id} aria-label={choice.label} onClick={() => onChoose(choice)} className="w-full text-left rounded-2xl border border-amber-200 bg-amber-50 hover:bg-amber-100 transition-colors px-4 py-4"><div className="font-black text-slate-900">{choice.label}</div><EffectSummary effect={choice.effect} /></button>)}</div></div></motion.section></div>; }