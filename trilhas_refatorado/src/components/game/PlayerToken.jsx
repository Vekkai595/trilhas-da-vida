import { motion } from 'framer-motion';
export default function PlayerToken({ x, y, emoji, color }) { return <motion.div animate={{ left: `${x}%`, top: `${y}%` }} transition={{ type: 'spring', stiffness: 60, damping: 14 }} className="absolute z-20 pointer-events-none" style={{ transform: 'translate(-50%, -50%)' }}><motion.div animate={{ scale: [1, 1.3, 1], opacity: [0.6, 0, 0.6] }} transition={{ repeat: Infinity, duration: 2.2 }} className="absolute inset-0 rounded-full -m-1" style={{ background: color, filter: 'blur(6px)' }} /><div className="relative flex items-center justify-center w-10 h-10 rounded-full border-2 border-white text-xl shadow-xl" style={{ backgroundColor: color }}>{emoji}</div></motion.div>; }